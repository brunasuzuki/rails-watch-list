<h1>Create a new list</h1>

<%# cria a tag form de html %>
<%# |f| significa form %>
<%= simple_form_for @list do |f| %>
  <%# tag de formulario que o simple_form criou %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit class: 'btn btn-success'%>
<%= end %>
